title = "gitleaks config"

[allowlist]
description = "Fake test credentials used in test fixtures"
regexes = [
  # Fake AK used in sanitization test fixtures (not a real credential)
  '''LTAItestFAKEnotREAL1234''',
]
